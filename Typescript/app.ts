// É uma superset que adiciona tipos para o JS.
// com o js não temos tipo, dai com o TS conseguimos ter tipo, mais completos.

function payroll(salary: number, bonus: number){
  return salary + bonus;
}

payroll(5000, 200)